# Vibe Coding Skill Structure v2.6

> **Last Updated:** 2026-05-11
> 이 파일은 자동으로 관리됩니다. 직접 수정하지 마세요.

---

## 스킬 구조

### 🔍 Debug 카테고리 (/debug-*)

| 스킬 | 설명 | 원본 이름 |
|-----|------|----------|
| `/debug` | 일반 디버깅, 버그 원인 파악 | `debug` |
| `/debug-perf` | 성능 병목, 최적화 | `bottleneck` |
| `/debug-web` | 프론트엔드 (JS, React, Vue) | `debug-web` |
| `/debug-css` | CSS 전용 (스타일, 레이아웃) | `debug-css` |
| `/debug-m` | 메모리 누수, OOM | `mem-check` |

### ✅ QA 카테고리 (/qa-*)

| 스킬 | 설명 | 원본 이름 |
|-----|------|----------|
| `/qa` | QA 테스트 (fix 포함) | `qa` |
| `/qa --report` | 보고서만 (수정 없음) | `qa --report` (플래그) |
| `/qa --quick` | 빠른 스모크 테스트 | — |

### 👁️ Review 카테고리 (/review-*)

| 스킬 | 설명 | 원본 이름 |
|-----|------|----------|
| `/review` | PR diff 리뷰 | `review` |
| `/review-code` | 코드 품질 (SOLID, 보안) | `review-code` |
| `/review-arch` | 아키텍처 리뷰 | `arch-review` |

### 📊 Analysis 카테고리

| 스킬 | 설명 |
|-----|------|
| `/impact` | 영향도 분석, 사이드 이펙트 예측 |
| `/complexity` | 코드 복잡도 분석 |

---

## 카테고리별 목적

| 카테고리 | 목적 | 질문 예시 |
|---------|------|----------|
| `/debug-*` | **원인 파악** | "왜 오류가 나나?" |
| `/qa` | **동작 확인** | "기능이 작동하나?" |
| `/review-*` | **코드 품질** | "코드가 깔끔한가?" |
| `/impact` | **영향도 분석** | "수정하면 어디 영향받아?" |

---

## 명확한 구분 기준

| 상황 | 사용할 스킬 |
|-----|-----------|
| 무엇이 문제인지 모르겠다 | `/debug` |
| 성능이 느리다 | `/debug-perf` |
| 메모리 문제다 | `/debug-m` |
| 화면이 깨진다 | `/debug-css` |
| JS/프론트 오류다 | `/debug-web` |
| 코드 머지 전 확인 | `/qa` + `/review` |
| 코드 품질 체크 | `/review-code` |
| 아키텍처 검토 | `/review-arch` |
| 수정 전 영향도 확인 | `/impact` |

---

## 스킬 분리 기준

1. **대상 분리**: web, mobile, api, db
2. **목적 분리**: perf(성능), security(보안), memory(메모리)
3. **단계 분리**: review(정적), test(동적), fix(수정)

---

## 🔄 스킬 자동 전환 규칙 (v2.6) 🆕

### 전환 규칙 테이블

| 요청 스킬 | 전환 조건 | 전환할 스킬 | 이유 |
|----------|----------|-----------|------|
| `/prd` | "기존 코드 수정", "리팩토링", "변경" | `/impact` | 기존 코드 수정은 영향도 분석 우선 |
| `/prd` | "버그", "오류", "안돼" | `/debug` | 버그는 원인 파악 우선 |
| `/prd` | "느려", "성능", "최적화" | `/bottleneck` | 성능 문제는 병목 분석 우선 |
| `/impact` | PRD 없이 "새 기능", "추가" | `/prd` | 새 기능은 기획 우선 |
| `/debug` | "느려", "성능" | `/bottleneck` | 성능 문제는 병목 분석 |
| `/debug` | "화면", "css", "스타일" | `/debug-css` | 프론트 스타일 전용 |
| `/review` | "버그", "고침" | `/debug` | 버그는 수정 전 원인 파악 |
| `/qa` | "보고서만", "수정하지 마" | `/qa --report` | 리포트 모드 |

### 전환 결정 트리

```
사용자 입력
    ↓
의도 파악
    ↓
문맥 분석
    ├─ 기존 코드 존재? → /impact
    ├─ 새 기능? → /prd
    ├─ 버그/오류? → /debug
    └─ 성능 문제? → /bottleneck
```

---

## END - 자동 관리 섹션
